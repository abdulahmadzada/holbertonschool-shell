#!/usr/bin/env bash
# Find the PID of the 4-to_infinity_and_beyond process
PID=$(pgrep -f "4-to_infinity_and_beyond")

# If the process exists, use gdb to force it to call exit(0)
if [ -n "$PID" ]; then
    echo "call exit(0)" | gdb -p "$PID" > /dev/null 2>&1
fi
