#!/usr/bin/env bash
# This script kills the process named 7-highlander and outputs OK if successful

if pgrep -f 7-highlander > /dev/null 2>&1; then
    pkill -f 7-highlander
    echo "OK"
else
    echo "7-highlander not running"
fi
